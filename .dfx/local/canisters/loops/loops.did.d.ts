import type { Principal } from '@dfinity/principal';
export interface _SERVICE {
  'for_loop' : (arg_0: Array<string>) => Promise<undefined>,
  'if_else' : (arg_0: bigint) => Promise<string>,
  'just_if' : (arg_0: boolean) => Promise<bigint>,
  'while_loop' : (arg_0: bigint) => Promise<undefined>,
}
